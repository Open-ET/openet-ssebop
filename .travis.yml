language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install pytest>=4.3.0 pytest-cov coverage codecov

script:
  - python -m pytest --cov=openet

after_success:
  - codecov
